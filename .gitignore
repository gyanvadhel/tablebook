# Dependencies
node_modules/

# Secrets — never commit real credentials
.env
.env.local
.env.*.local

# Legacy SQLite database; the app now uses Postgres
data/

# Personal documents that live in this folder but are not part of the project
reference-letter/

# Build & platform output
.next/
out/
.vercel/
dist/
build/

# Logs
*.log
npm-debug.log*

# Editor / OS noise
.DS_Store
Thumbs.db
.idea/
.vscode/

# Leftover on-disk uploads from before images moved into Postgres (uploads table)
public/uploads/

# Local experiments and image crops used while reverse-engineering floor plans
scratch/
scratch_crops/

# TypeScript incremental build cache
tsconfig.tsbuildinfo
